import styled from 'styled-components';
import { device } from '../helpers/devices';

const DisplaySpacerTop = styled.section`

  @media ${device.laptop} {
    margin-top: 50px;
  }
`

export default DisplaySpacerTop;